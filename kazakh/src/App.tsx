import { HashRouter as Router } from "react-router-dom";
import Routing from "./Routing";

function App() {
  return (
    <Router>
      <Routing></Routing>
    </Router>
  );
}

export default App;
